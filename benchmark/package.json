{
  "name": "ponder-benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "bun run src/index.ts",
    "create:app": "bun run src/create-app.ts"
  },
  "devDependencies": {
    "commander": "catalog:runtime",
    "drizzle-orm": "catalog:db",
    "pg": "catalog:db",
    "ponder": "workspace:*"
  }
}
