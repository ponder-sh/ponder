type OkpcToken @entity {
  id: ID!
  owner: String!
  lastOwner: String!
  nicknames: [OkpcOwnerKind!]
}

type OkpcOwner @entity {
  id: ID!
  address: String!
  kind: OkpcOwnerKind
}

enum OkpcOwnerKind {
  GOOD
  BAD
  UGLY
  REALLYUGLY
}
