type OkpcToken @entity {
  id: ID!
  tokenId: Int!
  owner: OkpcOwner!
}

type OkpcOwner @entity(immutable: true) {
  id: ID!
  address: String!
  traits: [OkpcOwnerTrait!]!
}

enum OkpcOwnerTrait {
  GOOD
  BAD
}
