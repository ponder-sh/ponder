type File @entity {
  id: String!
  name: String!
  size: Int!
  contents: String!
  createdAt: Int!
  # Metadata from our frontend, not guaranteed to be present
  type: String
  encoding: String
  compression: String
}
