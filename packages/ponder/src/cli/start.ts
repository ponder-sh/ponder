// import {
//   readHandlersTask,
//   readPonderConfigTask,
//   readSchemaTask,
//   runTask,
// } from "../core/tasks";

const start = async () => {
  // runTask(readPonderConfigTask);
  // runTask(readSchemaTask);
  // runTask(readHandlersTask);
};

export { start };
