{
  "name": "@ponder/ponder",
  "private": true,
  "engines": {
    "node": "16",
    "pnpm": "7"
  },
  "scripts": {
    "build": "esbuild `find tool \\( -name '*.ts' \\)` --platform=node --format=cjs --outdir=dist",
    "dev": "pnpm run build && node dist/server.js",
    "parser": "pnpm run build && node dist/configParser.js",
    "scraper": "pnpm run build && node dist/eventScraper.js",
    "prettier": "prettier --write src",
    "lint": "eslint src"
  },
  "dependencies": {
    "ethers": "^5.6.9",
    "express": "^4.18.1",
    "express-graphql": "^0.12.0",
    "graphql": "^16.2.0"
  },
  "devDependencies": {
    "@taskr/esnext": "^1.1.0",
    "@types/express": "^4.17.13",
    "esbuild": "^0.15.1"
  }
}
