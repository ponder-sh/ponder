{
  "name": "@ponder/ponder",
  "version": "0.0.17",
  "description": "API builder for blockchain apps",
  "license": "MIT",
  "author": "olias.eth",
  "files": [
    "dist"
  ],
  "bin": {
    "ponder": "./dist/bin/ponder"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "lint": "tsc && eslint src",
    "format": "prettier --write src",
    "compile": "esbuild `find src \\( -name '*.ts' \\)` --platform=node --format=cjs --outdir=dist && tsconfig-replace-paths --src src",
    "build": "pnpm run clean && pnpm run compile && mv dist/bin/ponder.js dist/bin/ponder",
    "release": "pnpm run build && VERSION=$(npm version patch) && npm publish && git add ./package.json && git commit -m \"release: $VERSION\""
  },
  "dependencies": {
    "@graphql-codegen/core": "^2.6.2",
    "@graphql-codegen/typescript": "^2.7.3",
    "@typechain/ethers-v5": "^10.0.0",
    "better-sqlite3": "^7.6.2",
    "commander": "^9.4.0",
    "dotenv": "^16.0.1",
    "esbuild": "^0.15.2",
    "ethers": "^5.6.9",
    "express": "^4.18.1",
    "express-graphql": "^0.12.0",
    "fastq": "^1.13.0",
    "graphql": "^15.3.0",
    "prettier": "^2.6.2",
    "typechain": "^8.0.0",
    "yaml": "^2.1.1"
  },
  "devDependencies": {
    "@ethersproject/abi": "^5.0.0",
    "@ethersproject/bytes": "^5.0.0",
    "@ethersproject/providers": "^5.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/express": "^4.17.13",
    "@types/node": "^18.7.8",
    "tsconfig-replace-paths": "^0.0.11",
    "typescript": "^4.5.5"
  },
  "engines": {
    "node": "16",
    "pnpm": "7"
  }
}
