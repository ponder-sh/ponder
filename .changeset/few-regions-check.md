---
"ponder": patch
---

Fixed a bug in JSON-RPC response validation logic that caused `RpcProviderError: Inconsistent RPC response data. The logs array contains two objects with 'logIndex' 0x39d (925)`.
