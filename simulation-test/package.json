{
  "name": "simulation-test",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "bun run --bun drizzle-kit generate",
    "migrate": "bun run --bun drizzle-kit migrate",
    "create:app": "bun run src/create-app.ts",
    "test": "bun run src/index.ts",
    "benchmark": "bun run src/benchmark.ts"
  },
  "devDependencies": {
    "@commander-js/extra-typings": "catalog:runtime",
    "@types/bun": "catalog:tooling",
    "@types/node": "catalog:",
    "@types/pg": "catalog:db",
    "@types/seedrandom": "catalog:tooling",
    "commander": "catalog:runtime",
    "drizzle-orm": "catalog:db",
    "pg": "catalog:db",
    "ponder": "workspace:*",
    "seedrandom": "catalog:runtime"
  }
}
