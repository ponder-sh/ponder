{
  "name": "ponder-benchmarks",
  "private": true,
  "scripts": {
    "up": "export $(grep -v '^#' .env.local | xargs) && docker-compose up",
    "bench": "tsup-node && export $(grep -v '^#' .env.local | xargs) && node dist/bench.mjs"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.51.1",
    "@graphprotocol/graph-ts": "^0.31.0",
    "@types/node": "^18.16.18",
    "execa": "^5.1.1",
    "node-fetch": "^2.6.11",
    "parse-prometheus-text-format": "^1.1.1",
    "tsup": "^7.0.0",
    "typescript": "^5.1.3",
    "viem": "0.3.50"
  }
}
